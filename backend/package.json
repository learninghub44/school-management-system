{
  "name": "cbc-school-erp",
  "version": "4.0.0",
  "description": "Kenya CBC School Management ERP — Production",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "db:migrate": "node -e \"require('dotenv').config(); const db=require('./config/db'); const fs=require('fs'); db.query(fs.readFileSync('./cbc_schema.sql','utf8')).then(()=>{ console.log('Schema applied'); process.exit(0); }).catch(e=>{ console.error(e); process.exit(1); });\"",
    "deploy": "wrangler deploy",
    "dev:worker": "wrangler dev"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "compression": "^1.8.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.3.1",
    "express-validator": "^7.2.0",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.0",
    "openai": "^6.44.0",
    "pg": "^8.12.0",
    "sanitize-html": "^2.17.5",
    "@neondatabase/serverless": "^0.9.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.4",
    "wrangler": "^3.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}